# 生产环境配置说明
我们需提供几种布署方案
## 方案一
所有的前后端项目都通过容器化布署在同一个vps上，系统提供一个脚本文件build-1.sh一键布署，一键布署应该自动更新打包容器和docker-compose环境、
配置ssl证书等。

## 方案二
前端布署在vercel，后端通过容器化布署在同一个vps，系统提供一个脚本文件build-2.sh一键布署，一键布署应该自动更新打包容器和docker-compose环境、
配置ssl证书等，并允许vercel前端访问后端服务。
